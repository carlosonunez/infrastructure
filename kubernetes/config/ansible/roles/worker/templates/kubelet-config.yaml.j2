kind: KubeletConfiguration
apiVersion: kubelet.config.k8s.io/v1beta1
authentication:
  anonymous:
    enabled: false
  webhook:
    enabled: true
  x509:
    clientCAFile: "/var/lib/kubernetes/ca.pem"
authorization:
  mode: Webhook
clusterDomain: "cluster.local"
clusterDNS:
  - "10.32.0.10"
podCIDR: "{{ pod_cidr_subnet_for_this_worker }}"
runtimeRequestTimeout: "15m"
tlsCertFile: "/var/lib/kubelet/{{ system_hostname }}.pem"
tlsPrivateKeyFile: "/var/lib/kubelet/{{ system_hostname }}-key.pem"
